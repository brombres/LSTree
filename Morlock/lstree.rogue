class LstreePackage : Package
  # Base class Package is defined here:
  # https://github.com/brombres/Morlock/blob/main/Source/Package.rogue
  PROPERTIES
    name = "brombres/lstree"

  METHODS
    method install
      download
      unpack
      build
      install_executable( &dest_filename="lst" )
endClass

